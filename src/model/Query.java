package model;

public class Query {

	/*protected static final String userByMail = "SELECT *"+
            " FROM Utente WHERE email= ?";*/
	
	/*protected static final String userById = "SELECT * "+
            " FROM Utente WHERE id=?";*/

	/*protected static final String registrazione = "INSERT INTO Utente (nome,cognome,email,password,dataDiNascita,sesso,amministratore)"+
            "VALUES (?,?,?,?,?,?,?)";*/
	
    //protected static final String categoriaById = "SELECT id, name FROM Categoria WHERE id=?";
    
    //protected static final String productByKey = "SELECT prodotto.id, prodotto.nome, prodotto.prezzo, prodotto.categoria, prodotto.descrizione FROM prodotto JOIN Categoria ON Categoria.ID=prodotto.categoria WHERE prodotto.id=?";
    
    //protected static final String categoriaAll = "SELECT id, nome FROM categoria";
    
    //protected static final String categoriaByProdotto = "SELECT categoria.id, categoria.nome FROM categoria JOIN prodotto on categoria.id=prodotto.categoria WHERE prodotto.id=?" ;
}